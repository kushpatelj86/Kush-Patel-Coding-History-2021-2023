#A tuple is a data structure where we can store multiple pieces of information but we can't change them/they are immutable, they are listed in parenthesis

coordinates = (4,5)

print(coordinates[0]) # we are acessing the value of a tuple, a tuple always start at position 0

#we can store tuples into lists

coordinates2 = [(4,5),(6,7),(80,34)]
print(coordinates2[0])