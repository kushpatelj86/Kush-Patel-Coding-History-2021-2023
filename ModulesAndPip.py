#there are so many modules and files that we can import , we would want to import them because it saves us time rathering than wastes time copying the method
import usefultools #uses all the videos and functions in the file

print(usefultools.roll_dice(10))

#to find modules search up a list of python modules
#https://docs.python.org/3/py-modindex.html